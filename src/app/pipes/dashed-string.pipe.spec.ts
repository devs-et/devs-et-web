import { DashedStringPipe } from './dashed-string.pipe';

describe('DashedStringPipe', () => {
  it('create an instance', () => {
    const pipe = new DashedStringPipe();
    expect(pipe).toBeTruthy();
  });
});
