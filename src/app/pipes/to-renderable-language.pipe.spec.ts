import { ToRenderableLanguagePipe } from './to-renderable-language.pipe';

describe('ToRenderableLanguagePipe', () => {
  it('create an instance', () => {
    const pipe = new ToRenderableLanguagePipe();
    expect(pipe).toBeTruthy();
  });
});
